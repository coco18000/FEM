#ifndef _BASE
#define _BASE

#include<vector>
#include<math.h>
#include "DxLib.h"

const static int DIV_NUM = 2;

const unsigned int WHITE = GetColor(255, 255, 255);
const unsigned int BLACK = GetColor(0, 0, 0);
const unsigned int RED = GetColor(0, 0, 255);
const unsigned int GREEN = GetColor(0, 255, 0);

const double MOUSE_RANGE = 100.0;
const double TIME_STEP = 0.15;
#endif